[project]
name = "ws-auth-lab"
version = "0.1.0"
description = "WebSocket auth: origin allowlist + single-use tickets"
readme = "README.md"
authors = [
    { name = "sanjayraj18", email = "22bsm052@iiitdmj.ac.in" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "pydantic-settings>=2.15.0",
    "redis>=8.1.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0"]
